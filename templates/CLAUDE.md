# Project Instructions

## Project Context

This project was generated by the Harness multi-agent system.
See `app_spec.md` for the full product specification.

> This file is automatically read by Claude Code at session start.
> It replaces custom prompt injection — zero code needed.

---

## MANDATORY: Agent Workflow

Every new agent session MUST follow this workflow:

### Step 1: Initialize Environment

```bash
chmod +x init.sh
./init.sh
```

This will:
- Install all dependencies
- Start development servers (frontend + backend)
- Print access URLs

**DO NOT skip this step.** Ensure servers are running before proceeding.

### Step 2: Select Next Task

Read `feature_list.json` and select ONE task to work on.

Selection criteria (in order of priority):
1. Choose a task where `passes: false`
2. Consider dependencies — fundamental features should be done first
3. Pick the highest-priority incomplete task

### Step 3: Implement the Task

- Read the task description and steps carefully
- Implement the functionality to satisfy all steps
- Follow existing code patterns and conventions

### Step 4: Test Thoroughly

After implementation, verify ALL steps in the task:

**Testing Requirements (MANDATORY):**

1. **Major page changes** (new pages, rewritten components, core interactions):
   - **MUST test in browser!** Use Playwright MCP tools
   - Verify page loads and renders correctly
   - Verify form submissions, button clicks, and interactions
   - Take screenshots to confirm UI is correct

2. **Minor code changes** (bug fixes, style tweaks, utility functions):
   - Can use linting/build verification
   - If in doubt, still do browser testing

3. **All changes must pass:**
   - Linting (no errors)
   - Build succeeds
   - Browser/functional testing verifies correctness

### Step 5: Update Progress

Write your work to `claude-progress.txt`:

```
## [Date] - Task: [task title]

### What was done:
- [specific changes made]

### Testing:
- [how it was tested]

### Notes:
- [any relevant notes for future agents]
```

### Step 6: Commit Changes

**IMPORTANT: All changes must be in the same commit, including feature_list.json update!**

1. Update `feature_list.json`: change task's `passes` from `false` to `true`
2. Update `claude-progress.txt` with work log
3. Commit everything together:

```bash
git add .
git commit -m "[task title] - completed and verified"
```

**Rules:**
- Only mark `passes: true` after ALL steps are verified
- Never remove or edit task descriptions/steps
- Never remove tasks from the list
- One task's changes (code + progress + feature_list) must be in the same commit

---

## ⚠️ Blocking Protocol

**If a task cannot be completed or tested, follow these rules:**

### Situations that require stopping:

1. **Missing environment config:**
   - `.env` needs real API keys
   - External services need accounts/setup

2. **External dependencies unavailable:**
   - Third-party API is down
   - OAuth flow requires human authorization
   - Service requires paid upgrade

3. **Testing impossible:**
   - Feature depends on external system not yet deployed
   - Requires specific hardware or environment

### When blocked — MUST DO:

**DO NOT (FORBIDDEN):**
- ❌ Make a git commit
- ❌ Set task's `passes` to `true`
- ❌ Pretend the task is complete

**DO (REQUIRED):**
- ✅ Record progress and blocking reason in `claude-progress.txt`
- ✅ Output clear blocking info explaining what human needs to do
- ✅ Stop the task and wait for human intervention

### Blocking info format:

```
🚫 BLOCKED — Human intervention needed

**Current task**: [task title]

**Work completed so far**:
- [what was done before blocking]

**Blocking reason**:
- [specific explanation]

**Human action required**:
1. [specific step 1]
2. [specific step 2]

**After unblocking**:
- Run [command] to continue
```

---

## Tech Stack

- Frontend: React + Vite + TypeScript
- Backend: FastAPI (Python)
- Database: SQLite
- Testing: Playwright (browser automation)

## Key Rules

1. **One task per session** — Focus on completing one task well
2. **Test before marking complete** — All steps must pass
3. **Browser testing for UI changes** — Major page changes MUST be tested in browser
4. **Document in claude-progress.txt** — Help future agents understand your work
5. **One commit per task** — All changes in a single commit
6. **Never remove tasks** — Only flip `passes: false` to `true`
7. **Stop if blocked** — Do NOT commit, output blocking info and stop
